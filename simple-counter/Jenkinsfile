pipeline{
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    // Install dependencies and build React app
                    bat 'npm install'
                    bat 'npm run build'
                }
            }
        }
    }
}
